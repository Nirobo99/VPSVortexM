# Production LiveKit config.
# Keys MUST match LIVEKIT_API_KEY / LIVEKIT_API_SECRET in .env
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true
keys:
  REPLACE_LIVEKIT_KEY: REPLACE_LIVEKIT_SECRET
logging:
  level: info
